{% set name = "napari-text-layer" %}
{% set version = "0.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/napari-text-layer-{{ version }}.tar.gz
  sha256: 93949dd7118a12babe79df21519fdcda18bfd63d2a7a899d7fcef61f692325f7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 1

requirements:
  host:
    - pip
    - python {{ python_min }}
  run:
    - napari
    - numpy
    - python >={{ python_min }}

test:
  imports:
    - napari_text_layer
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/napari-text-layer/
  summary: Text layer for bio-image annotation.
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/hanjinliu/napari-text-layer

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
    - hanjinliu
